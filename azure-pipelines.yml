trigger:
- master

pool:
  name: Default
  demands:
   - agent.name -equals Weltgeist  
steps:
- task: Maven@3
  inputs:
    mavenPomFile: 'pom.xml'
    goals: 'package'

- task: ShellScript@1
  inputs:
    scriptPath: aws.sh